import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Allow reading files from parent reports directory in development
  serverExternalPackages: [],
};

export default nextConfig;
